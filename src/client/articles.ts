import {searchArticles,type Article} from '../articles/related';
import {byId,el,link,button,fillForm} from './shared';
import {parseInteger} from '../search/state';
export function startArticles(){const articles:Article[]=JSON.parse(byId('article-data').textContent!),form=byId<HTMLFormElement>('article-form');form.querySelector('fieldset')!.disabled=false;
 const render=()=>{const q=new URLSearchParams(location.search),text=(q.get('q')??'').slice(0,100),category=['board','top'].includes(q.get('category')??'')?q.get('category')!:'',page=parseInteger(q.get('page')??'1',1,1000000)??1;fillForm(form,{q:text,category});const result=searchArticles(articles,text,category,page);byId('article-status').textContent=result.total+'件';const list=byId('article-list');list.replaceChildren();for(const a of result.items){const card=el('article');card.className='card';const h=el('h2');h.append(link(a.title,'/articles/'+a.slug+'/'));card.append(h,el('p',a.description),el('p','更新 '+a.updatedAt));list.append(card);}if(!result.total)list.append(el('p','条件に合う記事がありません'));const pages=byId('article-pages');pages.replaceChildren();for(const [label,n]of [['前の記事ページ',result.page-1],['次の記事ページ',result.page+1]] as const)if(n>=1&&n<=Math.ceil(result.total/12))pages.append(button(label,()=>{q.set('page',String(n));history.pushState(null,'','?'+q);render();}));};
 form.onsubmit=e=>{e.preventDefault();const q=new URLSearchParams();for(const [k,v]of new FormData(form))q.set(k,String(v));q.set('page','1');history.pushState(null,'','?'+q);render();};window.addEventListener('popstate',render);render();
}
